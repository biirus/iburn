export default ({ spacing, palette, colors }) => {
  return {
    root: {},

    highlighted: {
      backgroundColor: colors.b200a,
      color: colors.n400,

      borderRadius: 0,
    },

    dayWrapper: {
      overflow: "hidden",
    },

    selected: {},
  };
};
