export default ({ spacing, transitions, colors, }) => {
  return {
    root: {},

    popoverContent: {
      minWidth: "10em",
      // maxWidth: "20em",
      padding: [spacing.small, 0,],
    },

    item: {},
  };
};
